# @braedonsaunders/appkit-feedback

In-repo engine for the in-app product issue reporter: PII redaction, a short
triage toolset, and a host-injected GitHub publisher.

The generic package lives in the AppKit repo. This workspace copy is the
engine BeaconHS CI can resolve until the AppKit package is published to npm.
Keep the two in sync when the reporter contract changes.
