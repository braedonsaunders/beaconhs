# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
build/
.turbo/

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp

# Test coverage
coverage/
.nyc_output/

# TypeScript
*.tsbuildinfo

# Drizzle
drizzle/.snapshot/

# Legacy-migration data dumps — large schema/mapping JSON reproducible from the
# legacy MSSQL DB; kept local, not committed. (The .md docs beside them ARE kept.)
docs/migration/*.json

# Local storage
.r2-local/
uploads/
.local/
private/

# Misc
.cache/
*.local

# Local ops helpers that embed cluster creds — never commit.
packages/db/src/scripts/dokploy-*.ts

# Private legacy-migration ETL — lives on the migration Mac only (embeds MSSQL/Azure
# creds + bespoke mappers). Kept in the workspace for @beaconhs/db + @beaconhs/storage
# access; run on-demand. Never published. See memory: legacy-mssql-migration.
packages/etl/
